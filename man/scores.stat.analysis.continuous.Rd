% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores-continuous.R
\name{scores.stat.analysis.continuous}
\alias{scores.stat.analysis.continuous}
\title{Perform statistical analysis on scores against a continuous trait}
\usage{
scores.stat.analysis.continuous(
  scores,
  coldata,
  trait,
  method = c("pearson", "spearman", "fisher"),
  pval = 0.05
)
}
\arguments{
\item{scores}{A samples x features score matrix.}

\item{coldata}{A data frame containing clinical or experimental metadata for samples.
Must include the column specified in \code{trait}.}

\item{trait}{Character. The name of the (numeric) column in \code{coldata} representing the
continuous clinical or experimental trait to test against (e.g., age, tumor purity,
a continuous score, etc.).}

\item{method}{Character. Statistical test to perform. One of:
\itemize{
\item \code{"pearson"} - Pearson correlation (parametric, linear relationship)
\item \code{"spearman"} - Spearman rank correlation (non-parametric, monotonic relationship)
\item \code{"fisher"} - Fisher's exact test (both score and trait binarised at median)
}
Only one method is used per call; defaults to \code{"pearson"} if not specified.}

\item{pval}{Numeric. P-value threshold for significance (default: 0.05).}
}
\value{
A matrix of only the significant features (columns) after the chosen test.
Returns \code{NULL} if no significant features are found.
}
\description{
Unified interface for testing associations between any score matrix and a continuous
(numeric) clinical/experimental trait.
}
\details{
The function automatically calls the corresponding statistical test function
based on the \code{method} argument:
\itemize{
\item \code{scores.pearson.test()}
\item \code{scores.spearman.test()}
\item \code{scores.fisher.test.continuous()}
}

Each test produces both a statistical result and visual outputs (PDF plots)
stored in the \code{"Results/"} folder. These visualizations include the relevant
test results (p-values) annotated on the plots.
}
\examples{
\dontrun{
sig <- scores.stat.analysis.continuous(score_matrix, coldata, trait = "tumor_purity",
                                       method = "spearman", pval = 0.05)
}

}
