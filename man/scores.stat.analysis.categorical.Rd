% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores-categorical.R
\name{scores.stat.analysis.categorical}
\alias{scores.stat.analysis.categorical}
\title{Perform statistical analysis on scores against a categorical trait}
\usage{
scores.stat.analysis.categorical(
  scores,
  coldata,
  trait,
  method = c("ttest", "wilcox", "anova", "kruskal"),
  pval = 0.05
)
}
\arguments{
\item{scores}{A samples x features score matrix.}

\item{coldata}{A data frame containing clinical or experimental metadata for samples.
Must include the column specified in \code{trait}.}

\item{trait}{Character. The name of the column in \code{coldata} representing the categorical
clinical or experimental trait to test against (e.g., treatment arm, response group,
risk class).}

\item{method}{Character. Statistical test to perform. One of:
\itemize{
\item \code{"ttest"} - Welch's t-test (parametric, exactly 2 groups)
\item \code{"wilcox"} - Wilcoxon rank-sum test (non-parametric, exactly 2 groups)
\item \code{"anova"} - One-way ANOVA F-test (parametric, 2+ groups)
\item \code{"kruskal"} - Kruskal-Wallis test (non-parametric, 2+ groups)
}
Only one method is used per call; defaults to \code{"ttest"} if not specified.}

\item{pval}{Numeric. P-value threshold for significance (default: 0.05).}
}
\value{
A matrix of only the significant features (columns) after the chosen test.
Returns \code{NULL} if no significant features are found.
}
\description{
Unified interface for testing associations between any score matrix and a categorical
clinical/experimental trait, mirroring \code{scores.stat.analysis.continuous()} for the
categorical case.
}
\details{
The function automatically calls the corresponding statistical test function
based on the \code{method} argument:
\itemize{
\item \code{scores.ttest.test()}
\item \code{scores.wilcox.test()}
\item \code{scores.anova.test()}
\item \code{scores.kruskal.test()}
}

Each test produces both a statistical result and visual outputs (PDF boxplots)
stored in the \code{"Results/"} folder.
}
\examples{
\dontrun{
sig <- scores.stat.analysis.categorical(score_matrix, coldata, trait = "response_group",
                                        method = "kruskal", pval = 0.05)
}

}
