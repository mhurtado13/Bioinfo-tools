% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential-expression.R
\name{run_deg_analysis}
\alias{run_deg_analysis}
\title{Run differential expression analysis with edgeR/limma-voom}
\usage{
run_deg_analysis(
  counts,
  coldata,
  group_col,
  ref_level = NULL,
  pval = 0.05,
  logFC = 1,
  plot = TRUE,
  file.name = NULL
)
}
\arguments{
\item{counts}{A raw count matrix (genes x samples).}

\item{coldata}{A data frame of sample metadata whose row names match
the column names of \code{counts}.}

\item{group_col}{Character. Name of the column in \code{coldata} used as
the grouping factor for differential expression.}

\item{ref_level}{Character or \code{NULL}. Reference level for the group
factor. If \code{NULL}, the default factor ordering is used.}

\item{pval}{Numeric. Adjusted p-value cutoff used both to filter the
returned table (via \code{topTable(p.value = pval)}) and to mark
significance on the volcano plot. Default 0.05.}

\item{logFC}{Numeric. Absolute log2 fold-change cutoff marked on the
volcano plot (does not filter the returned table). Default 1.}

\item{plot}{Logical; if TRUE (default), saves a volcano plot to Results/
via \code{compute.volcano.plot()}.}

\item{file.name}{Optional character suffix used when saving the volcano
plot to \code{Results/}.}
}
\value{
A data frame of differentially expressed genes (p.adj < \code{pval}) as
returned by \code{limma::topTable}, with columns \code{logFC},
\code{AveExpr}, \code{t}, \code{P.Value}, \code{adj.P.Val}, and \code{B}.
}
\description{
Filters low-expression genes, applies TMM normalization, runs voom
transformation, fits a linear model, and returns the top differentially
expressed genes via \code{limma::topTable}. Optionally saves a volcano
plot (via \code{compute.volcano.plot()}) built from the full, unfiltered
set of tested genes.
}
\examples{
\dontrun{
deg <- run_deg_analysis(counts, coldata, group_col = "Group", ref_level = "Control")
}

}
