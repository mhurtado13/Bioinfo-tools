% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway-activity.R
\name{compute.pathway.activity}
\alias{compute.pathway.activity}
\title{Compute sample-level pathway activity scores}
\usage{
compute.pathway.activity(
  RNA.tpm,
  gene_sets = NULL,
  paths = NULL,
  pathway_source = c("PROGENy", "KEGG", "REACTOME", "Hallmark"),
  statistic = c("consensus", "aucell", "udt", "mdt", "wmean", "ulm", "mlm", "wsum",
    "viper", "gsva", "ora", "fgsea"),
  min_targets_size = 5,
  return = TRUE,
  file.name = NULL
)
}
\arguments{
\item{RNA.tpm}{A numeric matrix of log-normalized gene expression values -- log2(TPM + pseudocount), not raw
or linear-scale TPM -- with genes as rows and samples as columns (e.g. the output of
\code{compute.normalization(..., log = TRUE, zscore = FALSE)}).}

\item{gene_sets}{A list of gene sets (e.g., hallmark signatures or user-defined sets). If provided, activity scores are
computed for these sets (with \code{statistic}, like \code{pathway_source}) and saved to Results/ if \code{return} is TRUE, but are
not part of the value returned by this function -- only the \code{pathway_source} matrix is returned. Default is \code{NULL}.}

\item{paths}{A data frame describing the pathway-gene interactions for use with PROGENy (ignored for other \code{pathway_source} values). If \code{NULL}, the human PROGENy resource (top 500 genes) will be used by default.}

\item{pathway_source}{Character. The pathway resource used for the primary pathway score. Options are \code{"PROGENy"} (default, signed weights),
\code{"KEGG"}, \code{"REACTOME"}, or \code{"Hallmark"} (all three unsigned MSigDB gene set collections).}

\item{statistic}{Character. Which \code{decoupleR::decouple()} statistic to score \code{pathway_source} (and \code{gene_sets}, if given) with. Default \code{"consensus"}
(an ensemble score across the top-performing methods: MLM, ULM, and normalized WSUM). Any single underlying method
can be requested instead: \code{"aucell"}, \code{"udt"}, \code{"mdt"}, \code{"wmean"}, \code{"ulm"}, \code{"mlm"}, \code{"wsum"}, \code{"viper"}, \code{"gsva"},
\code{"ora"}, or \code{"fgsea"} (see \code{decoupleR::show_methods()}). \code{"mlm"} (and therefore the default \code{"consensus"}) fits
all sources jointly and can fail with a collinearity error on large, highly redundant collections (e.g. REACTOME) --
use a per-source method (e.g. \code{"ulm"}, \code{"viper"}, \code{"wsum"}) in that case.}

\item{min_targets_size}{Integer. Minimum number of target genes per source (pathway or gene set) present in \code{RNA.tpm}
required for it to be scored; sources with fewer are dropped. Default is 5.}

\item{return}{Logical; if TRUE, saves matrices in Results/ folder. Default is TRUE.}

\item{file.name}{Optional character suffix used when writing output CSV files.}
}
\value{
A scaled matrix of pathway activity scores for \code{pathway_source} (samples as rows, pathways as columns).
If \code{gene_sets} was also provided, its activity scores are saved to Results/ (when \code{return} is TRUE) but are not
included in this return value.
}
\description{
Computes pathway activity scores from normalized gene expression data. By default, the pathway
resource is PROGENy (Schubert et al., 2018), but KEGG, REACTOME, or MSigDB Hallmark of Cancer gene
sets can be used instead (\code{pathway_source}). Any of these can be scored by any
\code{decoupleR::decouple()} statistic (\code{statistic}), default \code{"consensus"}. Optionally, it also
scores any user-provided gene sets with that same statistic, independently of \code{pathway_source}.
}
\examples{
\dontrun{
# Compute only PROGENy activities
pathways <- compute.pathway.activity(counts.norm)

# Use REACTOME instead of PROGENy
pathways_reactome <- compute.pathway.activity(counts.norm, pathway_source = "REACTOME")
}

}
\references{
Schubert M, Klinger B, Kluenemann M, Sieber A, Uhlitz F, Sauer S, Garnett MJ, Bluethgen N, Saez-Rodriguez J.
Perturbation-response genes reveal signaling footprints in cancer gene expression. Nature Communications. 2018. \doi{10.1038/s41467-017-02391-6}

Badia-i-Mompel P., Vélez Santiago J., Braunger J., Geiss C., Dimitrov D., Müller-Dott S., Taus P., Dugourd A., Holland C.H., Ramirez Flores R.O. and Saez-Rodriguez J. 2022. decoupleR: ensemble of computational methods to infer biological activities from omics data. Bioinformatics Advances. https://doi.org/10.1093/bioadv/vbac016
}
