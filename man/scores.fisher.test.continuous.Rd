% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores-continuous.R
\name{scores.fisher.test.continuous}
\alias{scores.fisher.test.continuous}
\title{Fisher's exact test for two continuous variables (median-split)}
\usage{
scores.fisher.test.continuous(scores, coldata, trait, pval = 0.05)
}
\arguments{
\item{scores}{A samples x features score matrix. Continuous scores are binarised at the
median into High/Low groups.}

\item{coldata}{A data frame containing the clinical or experimental traits.}

\item{trait}{Character. Name of the (numeric) column in \code{coldata} to test with Fisher's
exact test. It is binarised at the median into High/Low groups.}

\item{pval}{Numeric. P-value threshold for significance (default 0.05).}
}
\value{
A matrix of only the significant features (columns) after the Fisher test, or
\code{NULL} if none are significant. Additionally, it saves corresponding barplot
visualizations in the "Results/" folder.
}
\description{
Both the score and the trait are binarised at their median into
High/Low groups, and Fisher's exact test is applied to the resulting 2x2 contingency table.
}
