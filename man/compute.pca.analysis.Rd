% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca-analysis.R
\name{compute.pca.analysis}
\alias{compute.pca.analysis}
\title{PCA analysis and visualization for a samples x features matrix}
\usage{
compute.pca.analysis(
  data,
  coldata = NULL,
  group_col = NULL,
  center = TRUE,
  scale = TRUE,
  file.name = NULL,
  return = TRUE
)
}
\arguments{
\item{data}{A samples x features numeric matrix or data frame. Zero-variance and
all-NA columns are dropped automatically.}

\item{coldata}{Optional data frame of sample-level annotations, with row names matching
the row names of \code{data}. Used to color samples in the PCA plot.}

\item{group_col}{Optional character. Column name in \code{coldata} used to color/group
samples in the PCA plot. Ignored if \code{coldata} is \code{NULL}.}

\item{center}{Logical. If TRUE (default), center features before PCA (see \code{stats::prcomp}).}

\item{scale}{Logical. If TRUE (default), scale features to unit variance before PCA.}

\item{file.name}{Optional character suffix used when saving the PCA plot to \code{Results/}.}

\item{return}{Logical. If TRUE (default), saves the PCA plot as a PDF in \code{Results/}.}
}
\value{
A list with:
\describe{
\item{pca}{The \code{stats::prcomp} object.}
\item{coords}{A data frame with the PC1/PC2 coordinates, \code{Sample}, and \code{Group} columns.}
\item{var_explained}{Percentage of variance explained by PC1 and PC2.}
\item{plot}{The \code{ggplot2} PCA scatter plot object.}
}
}
\description{
Runs a principal component analysis on any samples x features matrix, optionally colored by
a grouping column from \code{coldata}, and saves a PC1 vs. PC2 scatter plot to the
"Results/" folder.
}
